# Copy to .env and set this only if the Hugging Face dataset requires auth.
# Create a token at https://huggingface.co/settings/tokens after accepting
# access terms for builddotai/Egocentric-10K.
HF_TOKEN=

# Number of streamed video clips exported by the Docker exporter service.
SAMPLE_COUNT=5
